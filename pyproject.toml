[project]
name = "artemis-os"
version = "0.0.1"
description = "Artemis OS — Marketing Intelligence + Campaign Workflow System"
requires-python = ">=3.11"
readme = "README.md"
dependencies = [
    "fastapi[standard]>=0.115,<1.0",
    "uvicorn[standard]>=0.32",
    "sqlalchemy[asyncio]>=2.0",
    "asyncpg>=0.30",
    "alembic>=1.14",
    "pydantic>=2.10",
    "pydantic-settings>=2.6",
    "httpx>=0.28",
    "anthropic>=0.40",
    "pgvector>=0.3",
    "python-dotenv>=1.0",
    "sentence-transformers>=3.0,<4",
    "pyyaml>=6.0",
    "mcp>=1.7.0,<2.0",
    "apscheduler>=3.10,<4",
]

[dependency-groups]
dev = [
    "pytest>=8.3",
    "pytest-asyncio>=0.24",
    "pytest-cov>=6.0",
    "ruff>=0.8",
    "mypy>=1.13",
    "types-python-dateutil",
    "types-PyYAML",
]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "N", "SIM"]
ignore = ["E501"]

[tool.mypy]
python_version = "3.11"
strict = true
plugins = ["pydantic.mypy"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
# Both fixture and test loops use the same per-function scope. Session-scoped
# fixture loops cause "Future attached to a different loop" errors when
# function-scoped tests touch the fixture's asyncpg connections. NullPool on
# the test engine (see artemis/memory/tests/conftest.py) makes per-function
# loops safe — each test gets fresh connections and closes them cleanly.
asyncio_default_fixture_loop_scope = "function"
asyncio_default_test_loop_scope = "function"
testpaths = ["tests", "artemis"]
addopts = "-ra --strict-markers"

[tool.coverage.run]
source = ["artemis"]
branch = true

[tool.coverage.report]
show_missing = true
fail_under = 85
