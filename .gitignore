__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
.env
.env.*
!.env.example
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
*.egg-info/
dist/
build/
.idea/
.vscode/
.DS_Store

# Agent + sync scratch dirs (Sonnet Worker worktrees, clone shadows, sync state).
.claude/
.clone/
.sync/

# local runtime/agent junk
.app.log
.app.err.log
.smoke.app.log
.legislative_scout.log
.server-restart.log
.backups/
.codex-screenshots/
.claire/
Icon?
scripts/watchdog.conf

# Writing-sample PDFs — ~97MB of undiffable binaries kept on the Mini only.
# See docs/writing-samples-manifest.md for what the corpus contains.
writing-samples/

# MCP server configs: Vista Social embeds its API key in the server URL, so a
# committed .mcp.json would publish a live credential.
.mcp.json
